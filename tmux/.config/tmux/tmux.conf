# Load general options
source-file ~/.config/tmux/settings.conf

# Load status bar and theme customization
source-file ~/.config/tmux/theme.conf

# Load custom keybindings
source-file ~/.config/tmux/keybindings.conf
